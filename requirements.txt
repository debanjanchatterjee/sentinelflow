# This project uses pyproject.toml for dependency management.
# requirements.txt removed per repository policy — use:
#   python -m pip install "-e .[dev]"
# or install with your preferred tool (pipx, poetry, pip with -r generated file, etc.)
